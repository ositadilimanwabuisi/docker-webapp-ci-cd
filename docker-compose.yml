

services:

  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DB_HOST=database
    depends_on:
      - database

  database:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: osita
      POSTGRES_PASSWORD: devops2026
      POSTGRES_DB: devopsdb
    ports:
      - "5432:5432"